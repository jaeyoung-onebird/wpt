# WorkProof Work OS - 구현 로드맵

**프로젝트 목표**: 단순 근태 앱 → 노동 데이터 플랫폼(Work OS)으로 진화

**전체 기간**: 8-10주 (단계별 1주)

---

## 🎯 최종 비전

- 프리랜서가 월급제처럼 일정 관리
- 근무하면 WPT 보상 (게임화)
- NFT/등급/혜택으로 동기부여
- 기업은 WPT로 AI매칭/분석 사용
- 모든 활동 → 빅데이터 축적
- 청년 고용/실업 통계 도출

---

## 📊 전체 아키텍처

```
┌─────────────────────────────────────────────────┐
│             WorkProof Work OS                    │
├─────────────────────────────────────────────────┤
│                                                  │
│  Worker App              Admin App               │
│  ├─ Home                 ├─ Dashboard            │
│  ├─ Work (통합)          ├─ Events               │
│  ├─ Attendance           ├─ Workers              │
│  ├─ History (WPT+NFT)    ├─ Finance              │
│  └─ My                   └─ Settings             │
│                                                  │
├─────────────────────────────────────────────────┤
│              Core Systems                        │
├─────────────────────────────────────────────────┤
│  - Gamification Engine (WPT/Level/Streak)       │
│  - AI Matching Engine (점수 기반 추천)            │
│  - BigData Analytics (통계/지표)                 │
│  - Tokenomics (발행/소각/분배)                   │
└─────────────────────────────────────────────────┘
```

---

## 📋 10단계 구현 계획

---

### **Phase 1: DB 마이그레이션 및 백엔드 기반 구축**

**목표**: BigData & Gamification DB 스키마 배포

**기간**: 1주

**작업 내용**:
1. DB 마이그레이션 실행
   - `migrations/create_gamification_bigdata.sql` 실행
   - 8개 새 테이블 생성
   - Trigger/Function 설정

2. 백엔드 라우터 등록 확인
   - `/api/gamification` 엔드포인트 테스트
   - `/api/ai` 엔드포인트 테스트

3. 기존 데이터 마이그레이션
   - workers → worker_metrics 초기화
   - workers → worker_streaks 초기화

**파일**:
```
✓ migrations/create_gamification_bigdata.sql (생성 완료)
✓ src/api/routes/gamification.py (생성 완료)
✓ src/api/routes/ai_matching.py (생성 완료)
✓ src/api/main.py (라우터 등록 완료)
```

**테스트**:
- [ ] DB 테이블 8개 생성 확인
- [ ] Trigger 동작 확인 (attendance 추가 시 metrics 업데이트)
- [ ] API `/api/gamification/me/stats` 호출 테스트
- [ ] API `/api/ai/recommend-events` 호출 테스트

**배포 명령어**:
```bash
# DB 마이그레이션
psql -U postgres -d workproof_v2 -f migrations/create_gamification_bigdata.sql

# 백엔드 재시작
sudo systemctl restart workproof-backend
```

---

### **Phase 2: Gamification - 출석 보상 시스템**

**목표**: 출근/퇴근 시 WPT 자동 지급

**기간**: 1주

**작업 내용**:
1. Attendance 출근 시 WPT 지급 연동
   - `src/api/routes/attendance.py` 수정
   - 출근 완료 후 `/api/gamification/checkin-reward` 호출

2. 퇴근 시 근무시간 기반 보상
   - `/api/gamification/checkout-reward` 호출
   - 근무시간당 WPT 계산

3. 프론트엔드 보상 알림 UI
   - WPT 지급 애니메이션
   - Lottie/React-Spring 사용

**파일**:
```
수정: src/api/routes/attendance.py
새로: web/src/components/WPTRewardAnimation.jsx
수정: web/src/pages/Attendance.jsx
```

**API**:
- POST `/api/gamification/checkin-reward?attendance_id={id}`
- POST `/api/gamification/checkout-reward?attendance_id={id}`
- GET `/api/gamification/me/stats`

**테스트**:
- [ ] 출근 시 WPT 10 지급 확인
- [ ] 퇴근 시 근무시간 보너스 확인
- [ ] worker_metrics 테이블 업데이트 확인
- [ ] 애니메이션 표시 확인

**예상 효과**:
- 출근 동기부여 증가
- WPT 경제 활성화

---

### **Phase 3: Gamification - Streak & 레벨 시스템**

**목표**: 연속 출석, 레벨업 구현

**기간**: 1주

**작업 내용**:
1. Streak 계산 로직 완성
   - 연속 출석 3일마다 보너스 WPT
   - Longest streak 기록

2. 레벨 시스템 UI
   - 경험치 바 표시
   - 레벨업 애니메이션
   - 레벨별 혜택 표시

3. 리더보드 페이지
   - 전체 랭킹
   - 월간 랭킹
   - 내 순위 표시

**파일**:
```
새로: web/src/components/StreakCounter.jsx
새로: web/src/components/LevelBadge.jsx
새로: web/src/components/ExperienceBar.jsx
새로: web/src/pages/Leaderboard.jsx
```

**API**:
- GET `/api/gamification/me/stats` (streak 포함)
- GET `/api/gamification/leaderboard?period=month&limit=50`

**테스트**:
- [ ] 연속 출석 3일 보너스 확인
- [ ] 경험치 획득 시 레벨업 확인
- [ ] 리더보드 순위 표시 확인

**예상 효과**:
- 장기 근무 유도
- 경쟁 심리 활용

---

### **Phase 4: AI 매칭 - 기본 점수 계산**

**목표**: Worker-Event 매칭 점수 계산 로직 구현

**기간**: 1주

**작업 내용**:
1. 점수 계산 함수 고도화
   - 거리 점수 (실제 GPS 좌표 사용)
   - 신뢰도 점수 (완료율 + 평점)
   - 급여 적합도
   - 스킬 매칭
   - 가용성 체크

2. 매칭 로그 저장
   - ai_matching_logs 테이블 활용
   - 추천 정확도 추적

3. 가중치 설정 UI (Admin)
   - 거리/신뢰도/급여 비중 조절

**파일**:
```
수정: src/api/routes/ai_matching.py (점수 로직 개선)
새로: web/src/pages/admin/MatchingSettings.jsx
```

**API**:
- POST `/api/ai/calculate-score` (worker_id, event_id)
- GET `/api/ai/matching-stats` (Admin)

**테스트**:
- [ ] 점수 계산 로직 검증
- [ ] 가중치 변경 시 점수 변화 확인
- [ ] 매칭 로그 저장 확인

---

### **Phase 5: AI 매칭 - 추천 시스템**

**목표**: AI 기반 행사/근무자 추천

**기간**: 1주

**작업 내용**:
1. Worker: 추천 행사 리스트
   - 점수 70점 이상 자동 추천
   - "나에게 딱 맞는 행사" 섹션
   - 거리/급여/스킬 기반 필터

2. Admin: 추천 근무자 리스트
   - 행사에 맞는 근무자 추천
   - 신뢰도 순 정렬
   - WPT 차감 (기업 기능)

3. 자동 채우기 기능
   - "이번달 자동 채우기" 버튼
   - AI가 최적 일정 제안
   - 일괄 지원 (사용자 확인 필요)

**파일**:
```
수정: web/src/pages/Home.jsx (추천 섹션)
새로: web/src/components/RecommendationCard.jsx
수정: web/src/pages/admin/EventDetail.jsx (추천 근무자)
```

**API**:
- GET `/api/ai/recommend-events?limit=10&min_score=70`
- GET `/api/ai/recommend-workers/{event_id}?limit=20`
- POST `/api/ai/auto-fill-month?year=2026&month=2`

**테스트**:
- [ ] 추천 행사 표시 확인
- [ ] Admin 추천 근무자 확인
- [ ] 자동 채우기 동작 확인

**예상 효과**:
- 매칭 정확도 향상
- 지원률 증가

---

### **Phase 6: 프론트엔드 - Work 페이지 통합**

**목표**: Work 페이지 = List + Calendar 통합

**기간**: 1주

**작업 내용**:
1. Work 페이지 리디자인
   - 탭: List / Calendar
   - List: 내 지원/확정/급여
   - Calendar: 월간 스케줄

2. 월간 캘린더 컴포넌트
   - 확정 근무 표시
   - 추천 근무 표시 (점선)
   - 예상 수입 표시
   - 날짜 클릭 → 상세

3. 월 목표 설정
   - 목표 수입
   - 목표 근무일수
   - 달성률 표시

**파일**:
```
새로: web/src/pages/WorkOS.jsx (통합 페이지)
새로: web/src/components/WorkCalendar.jsx
새로: web/src/components/MonthlyGoal.jsx
리다이렉트: /work → /work (WorkOS)
리다이렉트: /calendar → /work?tab=calendar
```

**UI 구조**:
```
┌──────────────────────────────────┐
│ Work                              │
├──────────────────────────────────┤
│ [List] [Calendar]                │
├──────────────────────────────────┤
│ 이번달 목표: 200만원 (80%)        │
│ 근무일수: 12/15일                 │
├──────────────────────────────────┤
│ [Calendar View]                   │
│  1  2  3  4  5  6  7             │
│  [확정][추천]                     │
└──────────────────────────────────┘
```

**테스트**:
- [ ] 리스트/캘린더 토글 확인
- [ ] 월간 스케줄 표시 확인
- [ ] 예상 수입 계산 확인

---

### **Phase 7: 프론트엔드 - History 페이지**

**목표**: WPT + NFT + 이력 통합 페이지

**기간**: 1주

**작업 내용**:
1. History 페이지 생성
   - 섹션 1: WPT 잔액 & 거래 내역
   - 섹션 2: NFT 배지 컬렉션
   - 섹션 3: 근무 이력 & 통계
   - 섹션 4: 레벨 & 경험치

2. WPT 거래 내역
   - Earn/Spend/Burn 구분
   - 카테고리별 필터
   - 월별 통계 그래프

3. 통계 대시보드
   - 출석률
   - 평균 수입
   - 완료율
   - 신뢰도 점수

**파일**:
```
새로: web/src/pages/History.jsx
새로: web/src/components/WPTTransactionList.jsx
새로: web/src/components/WorkerStatsCard.jsx
리다이렉트: /wallet → /history
리다이렉트: /collection → /history
리다이렉트: /badges → /history
```

**API**:
- GET `/api/gamification/wpt/transactions?limit=50`
- GET `/api/gamification/me/stats`
- GET `/api/badges/me`

**테스트**:
- [ ] WPT 내역 표시 확인
- [ ] NFT 컬렉션 표시 확인
- [ ] 통계 그래프 표시 확인

---

### **Phase 8: 프론트엔드 - Attendance GPS 연동**

**목표**: 출퇴근 시 WPT 보상 UI 완성

**기간**: 1주

**작업 내용**:
1. GPS 출근 성공 시
   - WPT 지급 API 호출
   - 애니메이션 표시
   - "+10 WPT!" 팝업

2. Streak 표시
   - 연속 출석 N일
   - "3일 연속! 보너스 +5 WPT"
   - Progress bar

3. 경험치 획득
   - 레벨업 체크
   - "레벨업! Lv.3 → Lv.4" 애니메이션

**파일**:
```
수정: web/src/pages/Attendance.jsx
새로: web/src/components/WPTRewardAnimation.jsx
새로: web/src/components/LevelUpAnimation.jsx
```

**흐름**:
```
출근 버튼 클릭
   ↓
GPS/코드 인증
   ↓
출근 성공
   ↓
/api/gamification/checkin-reward 호출
   ↓
WPT 지급 + Streak 업데이트
   ↓
애니메이션 표시
```

**테스트**:
- [ ] 출근 시 WPT 애니메이션 확인
- [ ] Streak 카운터 증가 확인
- [ ] 레벨업 시 애니메이션 확인

---

### **Phase 9: Admin - Finance 대시보드**

**목표**: WPT 경제 관리 대시보드

**기간**: 1주

**작업 내용**:
1. Finance 페이지 생성
   - WPT 발행/소각 통계
   - 토크노믹스 지표
   - 근무자별 WPT 잔액
   - 거래 내역

2. WPT 관리 기능
   - Admin WPT 지급
   - WPT 정책 설정
   - 보상 금액 조정

3. 경제 지표
   - 총 발행량
   - 총 소각량
   - 유통량
   - 인플레이션율

**파일**:
```
새로: web/src/pages/admin/Finance.jsx
새로: web/src/components/admin/TokenomicsChart.jsx
새로: web/src/components/admin/WPTManagement.jsx
```

**API**:
- POST `/api/gamification/admin/grant-wpt` (지급)
- GET `/api/gamification/admin/analytics` (통계)
- PATCH `/api/admin/config/wpt-rewards` (설정)

**UI 섹션**:
```
┌────────────────────────────────┐
│ Finance Dashboard               │
├────────────────────────────────┤
│ 총 발행: 500,000 WPT           │
│ 총 소각: 150,000 WPT           │
│ 유통량: 350,000 WPT            │
├────────────────────────────────┤
│ [차트: 발행/소각 추이]          │
├────────────────────────────────┤
│ 근무자별 WPT 잔액               │
│ [리스트]                       │
└────────────────────────────────┘
```

**테스트**:
- [ ] 통계 표시 확인
- [ ] Admin WPT 지급 확인
- [ ] 정책 변경 확인

---

### **Phase 10: 최종 통합 및 테스트**

**목표**: 전체 시스템 통합 및 QA

**기간**: 1주

**작업 내용**:
1. 전체 플로우 테스트
   - 회원가입 → 지원 → 출근 → WPT 획득
   - AI 추천 → 지원 → 확정 → 근무
   - Admin 행사 생성 → 추천 근무자 → 출퇴근 관리

2. 성능 최적화
   - DB 인덱스 최적화
   - API 캐싱
   - 프론트엔드 번들 최적화

3. 문서 작성
   - API 문서 업데이트
   - 사용자 가이드
   - Admin 가이드

4. 배포 준비
   - 프로덕션 빌드
   - 환경변수 점검
   - 백업 설정

**체크리스트**:
- [ ] 전체 Worker 플로우 테스트
- [ ] 전체 Admin 플로우 테스트
- [ ] WPT 경제 균형 확인
- [ ] AI 추천 정확도 검증
- [ ] 성능 테스트 (100+ 동시 사용자)
- [ ] 모바일 반응형 확인
- [ ] 크로스 브라우저 테스트
- [ ] API 문서 완성
- [ ] 배포 스크립트 작성

**배포 순서**:
```bash
# 1. DB 백업
pg_dump workproof_v2 > backup_$(date +%Y%m%d).sql

# 2. DB 마이그레이션
psql -U postgres -d workproof_v2 -f migrations/create_gamification_bigdata.sql

# 3. 백엔드 배포
cd /home/ubuntu/workproof-chain-v2
git pull
sudo systemctl restart workproof-backend

# 4. 프론트엔드 빌드
cd web
npm run build

# 5. Nginx 재시작
sudo systemctl restart nginx

# 6. 헬스체크
curl https://workproof.co.kr/api/health
```

---

## 📊 7) Tokenomics 표

### Earn (WPT 획득)

| 활동 | 금액 | 조건 |
|------|------|------|
| 출근 | +10 WPT | GPS/코드 인증 |
| 퇴근 | +10 WPT | 정상 퇴근 |
| 근무시간 보너스 | +5 WPT/시간 | 실제 근무 시간 |
| 연속 출석 보너스 | +5 WPT | 3일마다 |
| 행사 완료 | +50 WPT | 평점 4.0+ |
| 완벽 출석 | +100 WPT | 월간 100% 출석 |
| 레벨업 보상 | +레벨×10 WPT | 레벨업 시 |
| Daily Check-in | +5 WPT | 앱 접속 보상 |

### Spend (WPT 사용)

| 활동 | 금액 | 혜택 |
|------|------|------|
| NFT 배지 (Basic) | -100 WPT | 업적 배지 |
| NFT 배지 (Rare) | -500 WPT | 희귀 배지 |
| NFT 배지 (Epic) | -1000 WPT | 전설 배지 |
| 상위 노출 | -50 WPT | 지원 우선 순위 |
| 프로필 부스트 | -30 WPT | 추천 확률 증가 (7일) |

### Burn (기업 WPT 소각)

| 기능 | 금액 | 효과 |
|------|------|------|
| AI 추천 근무자 | -20 WPT | 점수순 추천 |
| 급여명세서 다운로드 | -10 WPT/건 | Excel 다운 |
| 행사 보고서 | -10 WPT/건 | Excel 다운 |
| 분석 리포트 | -50 WPT/월 | 심화 분석 |
| 우선 매칭 | -30 WPT | 신뢰도 높은 근무자 우선 |
| Featured 행사 | -100 WPT/일 | 상단 노출 |

### Lock (스테이킹)

| 기간 | APY | 혜택 |
|------|-----|------|
| 1개월 | 5% | 배지 할인 10% |
| 3개월 | 12% | 배지 할인 20% + 우선 추천 |
| 6개월 | 25% | 배지 할인 30% + 우선 추천 + 전용 배지 |
| 12개월 | 50% | 배지 할인 50% + 최우선 추천 + 전용 배지 + VIP |

### 인플레이션 방지

- **목표**: 기업 소각량 > 근무자 획득량
- **평균 근무자**: 월 500 WPT 획득
- **평균 기업**: 월 1,000 WPT 소각
- **비율**: 1:2 (디플레이션 구조)

---

## 🎯 KPI 지표

### Worker KPI
- DAU (Daily Active Users)
- 평균 출석률
- WPT 획득량
- 레벨 분포
- Streak 평균

### Admin KPI
- 행사당 지원자 수
- 매칭 성공률
- AI 추천 정확도
- WPT 소각량
- 재고용률

### Platform KPI
- 총 거래량 (WPT)
- 유통량
- 디플레이션율
- 사용자 증가율
- 매출 (기업 WPT 판매)

---

## 🚀 성공 지표

### 3개월 후
- 활성 근무자 500명
- 월간 행사 100건
- WPT 유통량 500,000
- 평균 출석률 85%

### 6개월 후
- 활성 근무자 2,000명
- 월간 행사 500건
- WPT 유통량 2,000,000
- 기업 고객 50개

### 12개월 후
- 활성 근무자 10,000명
- 월간 행사 2,000건
- WPT 유통량 10,000,000
- 기업 고객 200개
- 정부 협약 체결

---

## 📝 참고 자료

### 기술 스택
- **Backend**: FastAPI, PostgreSQL, Python
- **Frontend**: React, Vite, TailwindCSS
- **Blockchain**: Ethereum, Web3.py
- **Analytics**: BigData tables, SQL aggregations
- **AI**: 점수 기반 추천 (향후 ML 확장)

### 파일 구조
```
workproof-chain-v2/
├─ migrations/
│  └─ create_gamification_bigdata.sql ✓
├─ src/api/routes/
│  ├─ gamification.py ✓
│  └─ ai_matching.py ✓
├─ web/src/
│  ├─ pages/
│  │  ├─ WorkOS.jsx (TODO)
│  │  ├─ History.jsx (TODO)
│  │  └─ admin/Finance.jsx (TODO)
│  └─ components/
│     ├─ WPTRewardAnimation.jsx (TODO)
│     ├─ WorkCalendar.jsx (TODO)
│     └─ StreakCounter.jsx (TODO)
└─ docs/
   └─ WORK_OS_ROADMAP.md ✓
```

### 다음 단계
1. **즉시**: Phase 1 DB 마이그레이션
2. **이번주**: Phase 2 출석 보상 시스템
3. **다음주**: Phase 3 Streak & 레벨

---

**작성일**: 2026-02-04
**버전**: 1.0
**작성자**: Claude Sonnet 4.5
